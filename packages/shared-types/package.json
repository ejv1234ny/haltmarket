{
  "name": "@haltmarket/shared-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./database": "./src/database.ts"
  },
  "scripts": {
    "lint": "echo 'no lint (types only)' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests' && exit 0",
    "clean": "rimraf .turbo node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
