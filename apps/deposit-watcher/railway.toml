[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/deposit-watcher/Dockerfile"

[deploy]
startCommand = "node /app/apps/deposit-watcher/dist/index.js"
healthcheckPath = "/healthz"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
